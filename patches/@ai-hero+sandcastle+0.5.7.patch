diff --git a/node_modules/@ai-hero/sandcastle/dist/WorktreeManager.js b/node_modules/@ai-hero/sandcastle/dist/WorktreeManager.js
index 61fbc34..b468ed3 100644
--- a/node_modules/@ai-hero/sandcastle/dist/WorktreeManager.js
+++ b/node_modules/@ai-hero/sandcastle/dist/WorktreeManager.js
@@ -115,11 +115,12 @@ export const create = (repoDir, opts) => Effect.gen(function* () {
         // Match by branch first; fall back to target path (covers mid-rebase
         // detached-HEAD state where the branch field is null).
         const existing = yield* listWorktrees(repoDir);
+        const normPath = (p) => p.replace(/\\/g, "/");
         const collision = existing.find((wt) => wt.branch === branch) ??
-            existing.find((wt) => wt.path === worktreePath);
+            existing.find((wt) => normPath(wt.path) === normPath(worktreePath));
         if (collision) {
             // Only reuse worktrees managed by sandcastle (under .sandcastle/worktrees/)
-            const isManagedWorktree = collision.path.startsWith(worktreesDir);
+            const isManagedWorktree = normPath(collision.path).startsWith(normPath(worktreesDir));
             if (isManagedWorktree) {
                 const dirty = yield* hasUncommittedChanges(collision.path);
                 if (dirty) {
diff --git a/node_modules/@ai-hero/sandcastle/dist/createSandbox.js b/node_modules/@ai-hero/sandcastle/dist/createSandbox.js
index fcb13ed..4b597fa 100644
--- a/node_modules/@ai-hero/sandcastle/dist/createSandbox.js
+++ b/node_modules/@ai-hero/sandcastle/dist/createSandbox.js
@@ -18,6 +18,7 @@ import { syncOut } from "./syncOut.js";
 import * as WorktreeManager from "./WorktreeManager.js";
 import { copyToWorktree } from "./CopyToWorktree.js";
 import { resolveCwd } from "./resolveCwd.js";
+import { patchGitMountsForWindows } from "./mountUtils.js";
 /**
  * @internal Builds a Sandbox handle with run() and interactive() methods.
  * The close callback controls teardown behavior — top-level createSandbox()
@@ -252,7 +253,10 @@ export const createSandboxFromWorktree = async (options) => {
             });
         }
         else {
-            startEffect = resolveGitMounts(join(hostRepoDir, ".git")).pipe(Effect.provide(NodeFileSystem.layer), Effect.catchAll(() => Effect.succeed([])), Effect.flatMap((gitMounts) => startSandbox({
+            startEffect = resolveGitMounts(join(hostRepoDir, ".git")).pipe(Effect.provide(NodeFileSystem.layer), Effect.catchAll(() => Effect.succeed([])), Effect.flatMap((gitMounts) => Effect.tryPromise({
+                try: () => patchGitMountsForWindows(gitMounts, worktreePath, SANDBOX_REPO_DIR),
+                catch: () => gitMounts,
+            })), Effect.flatMap((gitMounts) => startSandbox({
                 provider,
                 hostRepoDir,
                 env,
@@ -366,7 +370,10 @@ export const createSandbox = async (options) => {
             });
         }
         else {
-            startEffect = resolveGitMounts(join(hostRepoDir, ".git")).pipe(Effect.provide(NodeFileSystem.layer), Effect.catchAll(() => Effect.succeed([])), Effect.flatMap((gitMounts) => startSandbox({
+            startEffect = resolveGitMounts(join(hostRepoDir, ".git")).pipe(Effect.provide(NodeFileSystem.layer), Effect.catchAll(() => Effect.succeed([])), Effect.flatMap((gitMounts) => Effect.tryPromise({
+                try: () => patchGitMountsForWindows(gitMounts, worktreePath, SANDBOX_REPO_DIR),
+                catch: () => gitMounts,
+            })), Effect.flatMap((gitMounts) => startSandbox({
                 provider,
                 hostRepoDir,
                 env,
diff --git a/node_modules/@ai-hero/sandcastle/dist/sandboxes/docker.js b/node_modules/@ai-hero/sandcastle/dist/sandboxes/docker.js
index 4c2fc35..1612a50 100644
--- a/node_modules/@ai-hero/sandcastle/dist/sandboxes/docker.js
+++ b/node_modules/@ai-hero/sandcastle/dist/sandboxes/docker.js
@@ -98,6 +98,10 @@ export const docker = (options) => {
                             const rl = createInterface({ input: proc.stdout });
                             rl.on("line", (line) => {
                                 stdoutChunks.push(line);
+                                // Keep only the last 500 lines to prevent RangeError on long-running agents
+                                if (stdoutChunks.length > 500) {
+                                    stdoutChunks.shift();
+                                }
                                 onLine(line);
                             });
                         }
diff --git a/node_modules/@ai-hero/sandcastle/dist/sandboxes/no-sandbox.js b/node_modules/@ai-hero/sandcastle/dist/sandboxes/no-sandbox.js
index c1a07c4..2352958 100644
--- a/node_modules/@ai-hero/sandcastle/dist/sandboxes/no-sandbox.js
+++ b/node_modules/@ai-hero/sandcastle/dist/sandboxes/no-sandbox.js
@@ -50,6 +50,10 @@ export const noSandbox = (options) => ({
                         const rl = createInterface({ input: proc.stdout });
                         rl.on("line", (line) => {
                             stdoutChunks.push(line);
+                            // Keep only the last 500 lines to prevent RangeError on long-running agents
+                            if (stdoutChunks.length > 500) {
+                                stdoutChunks.shift();
+                            }
                             opts.onLine(line);
                         });
                     }
